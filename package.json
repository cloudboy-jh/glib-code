{
  "name": "glib-code",
  "private": true,
  "workspaces": [
    "server",
    "web",
    "desktop",
    "shared"
  ],
  "scripts": {
    "dev": "bun run --cwd server dev",
    "dev:server": "bun run --cwd server dev",
    "dev:web": "bun run --cwd web dev",
    "build": "bun run --cwd shared build && bun run --cwd server build && bun run --cwd web build && bun run --cwd desktop build",
    "check": "bun run --cwd shared check && bun run --cwd server check && bun run --cwd web check && bun run --cwd desktop check"
  }
}
