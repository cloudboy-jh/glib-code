# Next Steps

Release is on hold until GitHub connect is verified in a real packaged build.

1. Build the packaged app (`bun run build`).
2. Test GitHub connect end-to-end in the packaged app.
3. Cut the release once connect is confirmed working.
